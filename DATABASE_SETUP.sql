-- خطوات إنشاء قاعدة بيانات ABC
-- ================================

-- 1. افتح pgAdmin من قائمة Start
-- 2. اضغط على السيرفر (Servers)
-- 3. اضغط كليك يمين على "Databases"
-- 4. اختر "Create" > "Database"

-- في نافذة إنشاء قاعدة البيانات:
-- Name: abc_constructions
-- Owner: postgres

-- ثم اضغط "Save"

-- أو يمكنك كتابة هذه الأوامر في Query Tool:
CREATE DATABASE abc_constructions;
